# acmed v0.2.7 Release Notes

Release date: 2026-04-26

## Highlights

- Restored Docker smoke CI compatibility after addon-runtime refactor.

## Changes

- `docker/Dockerfile` now exposes `runtime-base` again for legacy CI targets.
- Added `runtime` alias stage to preserve newer target naming.
- Defaulted `ACMED_SOURCE_DIR` to `.` so upstream builds work without extra
  build args.
